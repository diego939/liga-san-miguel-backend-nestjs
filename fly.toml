# fly.toml app configuration file generated for liga-san-miguel-backend-nestjs on 2026-05-03T06:01:50Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'liga-san-miguel-backend-nestjs'
primary_region = 'iad'

[build]

[deploy]
  release_command = 'npx prisma migrate deploy'
  seed_command = 'ts-node --compiler-options {"module":"CommonJS"} prisma/seed.ts'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
